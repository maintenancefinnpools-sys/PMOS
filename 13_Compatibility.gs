/**
 * PMOS compatibility layer.
 *
 * Existing global public functions and trigger handlers remain unchanged.
 * Future namespaced APIs may delegate through this module without breaking
 * installed triggers, menu callbacks, or google.script.run clients.
 */
